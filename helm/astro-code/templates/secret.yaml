{{- if .Values.opencode.serverPassword }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "astro-code.fullname" . }}-secret
  labels:
    {{- include "astro-code.labels" . | nindent 4 }}
type: Opaque
stringData:
  OPENCODE_SERVER_PASSWORD: {{ .Values.opencode.serverPassword | quote }}
{{- end }}
