image:
  repository: crpi-wixjy6gci86ms14e.cn-hongkong.personal.cr.aliyuncs.com/ay-dev/astro-code
  tag: v20260407-135357
  pullPolicy: Always


replicaCount: 1

ingress:
  enabled: false

service:
  type: NodePort
  nodePort: 31634

autoscaling:
  enabled: false

hostAliases:
  - ip: "192.168.31.111"
    hostnames:
      - "catalog.euclid.mcp.ay.dev"
      - "fits-cutout.mcp.ay.dev"

opencodeConfig:
  mode: external
  external:
    secretName: astro-code-opencode-external

runtime:
  interactionBackend: "native"

persistence:
  enabled: true
  storageClass: local-path
  accessMode: ReadWriteOnce
  config:
    enabled: true
    size: 1Gi
  data:
    enabled: true
    size: 1Gi

opencode:
  serverPassword: ""
  aiModelKey: ""

extraEnv:
  - name: MCP_INSECURE_TLS
    value: "0"
  - name: NODE_TLS_REJECT_UNAUTHORIZED
    value: "0"

extraVolumes:
  []

extraVolumeMounts:
  []
